# 值类型 vs 引用类型

JS内存管理的基石

## 看点

- let a = b；为何有时同步变化，有时又各自独立？
- 揭秘JS函数传参的底层真相
- const声明的对象，为何还能被修改？

### 是什么

JS如何在内存中存储数据？

### 值类型

- 变量，直接存储数据的值
- 赋值，是值的拷贝
- 存储位置：栈（Stack）

成员：String Number Boolean Null Undefined Symbol BigInt

### 引用类型

- 变量，存储的是内存地址（引用）
- 赋值，是引用的拷贝
- 数据本身存放于：堆（Heap）

成员：Object Array Function等等

## 怎么用

### - 值类型的赋值行为

```
let a = 100;
// fragment
let b = a;
// fragment
a = 200;
// fragment
console.log(a);// 200
console.log(b);// 100
```

### - 引用类型的赋值行为

```
let obj1 = { name: 'Alice'};

let obj2 = obj1;

obj1.name = 'Bob';

console.log(obj1.name);// Bob
console.log(obj2.name);// Bob
```

## 注意啥

### - 考点一：函数参数传递

- JS永远是按值传递
- 值类型：传递值的副本
- 引用类型：传递引用的副本

```
function modify(obj) {
	//修改属性，会影响外部
	obj.name = 'Modified';
//fragment
	//重新赋值，不会影响外部
	obj = { name: 'New Object'};
}
// fragment
let myObj = { name:'Initial'};
modify(myObj);
console.log(myObj.name);// 'Modified'
```



### - 考点二：const的不变性

- const保证的是变量存储的地址不可变
- 不保证地址指向的对象内容不可变

```
const user = { name: 'John'};
user.name = 'Jane';//合法 修改对象内部属性

//非法 尝试修改user变量的地址
//user = { name: 'Doe'};//TypeError
```



### - 考点三：相等性判断（===）

- 值类型：比较值是否相等
- 引用类型：比较引用/地址是否相等

```
//值类型比较
console.log(100 === 100);// true

//引用类型比较
console.log({} === {});// false(两个独立的地址)

// fragment
let objA = { id: 1};
let objB = objA;
console.log(objA === objB);//true(指向同一个地址)
```

