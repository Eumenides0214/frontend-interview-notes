# 一、var、const、let有什么区别？

## **看点：**

- var声明变量，到底有什么历史包袱？

- let和const如何解决这些问题？
- 什么是“暂时性死区（TDZ）”？
- 面试官想听到的标准答案是什么？

### **1.var的核心特性：**

- 作用域：函数作用域 或 全局作用域
- 变量提升：声明被提升，初始化为undefined
- 重复声明:允许
- 可变性：可以重新赋值

#### var的痛点（1/2)

变量提升(HOISTING)

- 在声明前访问，不报错，值为undefined
- 违反编码直觉，易导致bug

#### var的痛点（2/2)

缺乏块级作用域

- if块内的变量会“泄露”到外部
- for循环中的变量会“泄露”到外部

#### 代码解读：var的行为

```
function varExample(){
	console.log(a);//fragment:提升，值为undefined

    var a = 10;
    console.log(a);//fragment：赋值后，值为10

    if (true）{
    var a = 20;//fragment：覆盖了外层的a
    console.log(a);//fragment:输出20
    }

    console.log(a);//fragment：值被if块修改，输出20
}
```

### 2.ES6的解决方案：let & const

#### ①let:亲新一代变量声明

- 作用域：块级作用域（{...}）
- 暂时性死区（TDZ)：声明前无法访问
- 重复声明：同一作用域内不允许
- 可变性：可以重新赋值

#### 代码解读：let的块级作用域

```
function letExample() {
    //console.log(b);//ReferenceError:TDZ
    
    let b = 10;
    console.log(b);//fragment:输出 10
    
    if (true) {
    let b = 20;//fragment:这是if块内的新变量
    console.log(b);//fragment:输出 20
    }
    
    console.log(b);//fragment:不受if块影响，输出10
}
```

#### ②const：声明即永恒

- 作用域：块级作用域（{...}）
- 暂时性死区（TDZ）：声明前无法访问
- 可变性：不能重新赋值(Must be initialized)
- 核心：保证变量引用的不变性

#### 代码解读：const的基本用法

```
function constExample() {
    const C = 30;
    
    // c = 40;// TypeError!
}
```

#### const与引用类型

允许修改内部

const保证的是引用不变

```
const obj = { name: 'React'};

fragment
//修改对象内部的属性？-> 允许!
obj.name = 'Vue';
console.log(obj.name);//'Vue'

//fragment
//将 obj 指向一个新对象？ -> 不允许！
obj = {};//TypeError!
```

### 3.关键概念深挖

#### ①关键点1：暂时性死区(TDZ)

- 适用于let和const
- 区域：从作用域{开始，到变量声明let/const...结束
- 行为：在此区域内访问变量，抛出ReferenceError

#### ②关键点2：全局对象

- var:在全局作用域声明，会成为window的属性

```
var globalvar ="I'm everywhere";
console.log(window.globalvar);// "I'm everywhere"
```

- let/const:不会成为window的属性

```
let globalLet ="Scoped";
console.log(window.globalLet);// undefined
```

（减少了对全局对象的污染）

#### ③关键点3:开发最佳实践

1.优先使用const

- 增强代码可预测性，避免意外修改。

2.当变量需要重新赋值时，使用let

- 例如：循环计数器、状态切换。

3.告别var

- 在现代JS项目中，应避免使用var。

## <span style="color: red;">面试现场</span>

<span style="color: blue;">**Q：请谈谈var、let、const的区别？**</span>

**A：第一，最主要的区别在于作用域规则不同，var声明的变量是函数作用域或者全局作用域，而let和const是块级作用域，块级作用域的引入解决了过去var在if语句或者for循环中容易造成变量泄露的问题，让代码更健壮；第二，var存在变量提升，它的声明会提到作用域顶部，并初始化为undefined，这就意味着在声明前访问它就不会报错，但会得到undefined，相比之下，另外两个不存在变量提升，它们有暂时性死区的特性，在声明语句出现之前访问它会直接抛出错误，得到一个Reference Error，这是一种更安全，更符合预期的行为；第三，从赋值和声明规则来看，var既可以重新声明，也可以重新赋值，let不能在同一个作用域中重复声明，但可以重新赋值，而const是最严格的，两者都不能；最后，特别的，对于const需要强调的是，当它用于声明对象或数组这些引用类型时，它保证的是变量的引用的不变，而对象内部的属性是可变的。**

