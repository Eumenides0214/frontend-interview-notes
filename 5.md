# 数组常用方法背后的面试点

## 看点

- map,filter,reduce：不只是会用
- splicevsslice：一字之差，天壤之别
- 可变性(Mutability)：React开发的“红线”

### 核心分类

- 迭代方法：forEach,map,filter,reduce...
- 修改原数组(Mutator)：push, pop, splice...
- 返回新数组(Non-mutating)：slice,concat,map...

### map（）是什么？

- 核心功能：转换数组，创建新数组
- 返回值：一个等长的新数组
- 改变原数组?：否(Non-mutating)

### filter（）是什么？

- 核心功能：筛选数组，创建新数组
- 返回值：一个新的、长度不定的数组
- 改变原数组?：否(Non-mutating)

### reduce（）是什么？

- 核心功能：汇总/累加数组元素
- 结果：返回单个汇总值（The"Reducer")
- 改变原数组?：否(Non-mutating)

**reduce（） API**

arr.reduce(callback, initialValue)

**callback函数**

callback(accumulator, currentValue, currentIndex, array)
accumulator:累加器
currentValue:当前值

### splice（）：数组的“手术刀”

- 核心功能：原地修改数组（增/删/改）
- 返回值：被删除元素的数组
- 改变原数组?：是(Mutator Method)

插入、删除

## 面试高频：map VS forEach

核心区别是什么？

### 对比1：返回值

- map：返回一个新数组
- forEach：返回undefined

### 对比2：用途

- map：用于数据转换
- forEach：用于执行无返回值的操作

### 对比3：链式调用

- map可以链式调用：arr.map(...) .filter(...)
- forEach不可以

**reduce的initialValue**

提供 vs 不提供 有何区别？

**提供：**

- accumulator初始值为initialvalue
- 遍历从数组第一个元素（index：0)开始

**不提供：**

- accumulator初始值为数组第一个元素
- 遍历从数组第二个元素（index：1）开始
- 注意：对空数组调用会抛出TypeError!

### 核心考点：可变性

- 哪些方法会“原地”修改数组？
- 为什么这在React
- 中很重要？

### 修改原数组

- push,pop,shift,unshift
- splice
- sort,reverse
- 原则：避免直接在React State上使用

### 不修改原数组

- map,filter,reduce
- slice,concat
- 原则：在React中创建新状态的首选

### slice VS splice

#### slice（切片）

- 功能：返回数组的浅拷贝片段
- 副作用：无，不修改原数组

#### splice（手术）

- 功能：添加/删除/替换元素
- 副作用：有，修改原数组

### 核心总结

- 分清可变性：splice修改原数组，slice/map/filter返回新数组。
- map vsforEach：核心区别在于返回值和用途。
- reduce：注意initialvalue对启动行为和空数组处理的影响。